import { useEffect, useState } from "react";
import { booksAPI } from "../api";
import { Icon, Spinner, EmptyState } from "../components/UI";
import { useApp } from "../context/AppContext";

const empty = { title: "", author: "", category: "General", description: "", resourceUrl: "", resourceType: "link", coverUrl: "", published: true };
export default function AdminBooks() {
  const { showToast } = useApp(); const [books, setBooks] = useState([]); const [form, setForm] = useState(empty); const [file, setFile] = useState(null); const [editing, setEditing] = useState(null); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false);
  const load = () => booksAPI.adminAll().then((r) => setBooks(r.data || [])).catch(() => {}).finally(() => setLoading(false));
  useEffect(load, []);
  const save = async (e) => { e.preventDefault(); if (!form.title.trim() || (!form.resourceUrl.trim() && !file)) return showToast("Title and either a resource URL or file are required.", "error"); setSaving(true); try { let payload = { ...form }; if (file) { const uploadData = new FormData(); uploadData.append("file", file); const uploaded = await booksAPI.upload(uploadData); payload = { ...payload, resourceUrl: uploaded.data.resourceUrl, fileName: uploaded.data.fileName, resourceType: uploaded.data.resourceType }; } const r = editing ? await booksAPI.update(editing, payload) : await booksAPI.create(payload); setBooks((items) => editing ? items.map((x) => x._id === editing ? r.data : x) : [r.data, ...items]); setForm(empty); setFile(null); setEditing(null); showToast("Book saved."); } catch (err) { showToast(err.response?.data?.message || "Could not save book.", "error"); } finally { setSaving(false); } };
  const remove = async (id) => { if (!window.confirm("Delete this book?")) return; await booksAPI.delete(id); setBooks((items) => items.filter((x) => x._id !== id)); showToast("Book deleted."); };
  return <div style={{ padding: 28, maxWidth: 1100 }}><div style={{ marginBottom: 24 }}><h3>Book Library</h3><p style={{ color: "var(--gray-mid)", fontSize: 14 }}>Add PDFs, DOC/DOCX resources, or approved external links for visitors.</p></div><form className="card" style={{ padding: 24, marginBottom: 24 }} onSubmit={save}><h4>{editing ? "Edit Book" : "Add Book"}</h4><div className="grid-2"><div className="form-group"><label>Title</label><input value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} /></div><div className="form-group"><label>Author</label><input value={form.author} onChange={(e) => setForm({ ...form, author: e.target.value })} /></div><div className="form-group"><label>Resource URL</label><input type="url" value={form.resourceUrl} onChange={(e) => setForm({ ...form, resourceUrl: e.target.value })} placeholder="https://…" /></div><div className="form-group"><label>Upload PDF, DOC, or DOCX</label><input type="file" accept=".pdf,.doc,.docx" onChange={(e) => setFile(e.target.files?.[0] || null)} /></div><div className="form-group"><label>Type</label><select value={form.resourceType} onChange={(e) => setForm({ ...form, resourceType: e.target.value })}><option value="link">External link</option><option value="pdf">PDF</option><option value="doc">DOC</option><option value="docx">DOCX</option></select></div><div className="form-group"><label>Category</label><input value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} /></div><div className="form-group"><label>Cover URL (optional)</label><input type="url" value={form.coverUrl} onChange={(e) => setForm({ ...form, coverUrl: e.target.value })} /></div></div><div className="form-group"><label>Description</label><textarea rows={3} value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} /></div><div style={{ display: "flex", gap: 10 }}><button className="btn btn-primary" disabled={saving}>{saving ? <Spinner size={15} color="white" /> : <Icon name="check" size={15} />} {editing ? "Update Book" : "Add Book"}</button>{editing && <button type="button" className="btn btn-ghost" onClick={() => { setEditing(null); setFile(null); setForm(empty); }}>Cancel</button>}</div></form>{loading ? <Spinner /> : books.length === 0 ? <EmptyState icon="bookOpen" title="No books yet" /> : <div className="card" style={{ padding: 18 }}>{books.map((b) => <div key={b._id} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 14, padding: "14px 0", borderBottom: "1px solid var(--gray-light)" }}><div><strong>{b.title}</strong><div style={{ color: "var(--gray-mid)", fontSize: 13 }}>{b.author || "No author"} · {b.resourceType} · {b.published ? "Published" : "Hidden"}</div></div><div style={{ display: "flex", gap: 8 }}><button className="btn btn-ghost btn-sm" onClick={() => { setEditing(b._id); setForm({ ...empty, ...b }); }}>Edit</button><button className="btn btn-danger btn-sm" onClick={() => remove(b._id)}>Delete</button></div></div>)}</div>}</div>;
}
